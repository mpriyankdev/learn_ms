spring:
  application:
    name: sample_config
  cloud:
    config:
      uri: http://localhost:12500
      fail-fast: true
server:
  port: 12501

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:12502/eureka/